/**
 * TV-Parity 500 Tests — Sine Line (2-anchor drag).
 */
import { register500ToolSuite } from "./tv-parity-500-factory";

register500ToolSuite({
  variant: "sineLine",
  testId: "tool-sineLine",
  railTestId: "rail-patterns",
});
