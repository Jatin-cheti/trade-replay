/** TV-Parity 500 Tests — INFO LINE (2 anchors, segment + info badge). */
import { register500ToolSuite } from "./tv-parity-500-factory";
register500ToolSuite({ variant: "infoLine", testId: "tool-info-line" });
