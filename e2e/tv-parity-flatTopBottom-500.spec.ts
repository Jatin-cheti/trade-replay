/**
 * TV-Parity 500 Tests — Flat top/bottom channel (2-anchor drag).
 */
import { register500ToolSuite } from "./tv-parity-500-factory";

register500ToolSuite({
  variant: "flatTopBottom",
  testId: "tool-flat-top-bottom",
});
