/**
 * TV-Parity 500 Tests — Parallel channel (2-anchor drag).
 */
import { register500ToolSuite } from "./tv-parity-500-factory";

register500ToolSuite({
  variant: "channel",
  testId: "tool-parallel-channel",
});
