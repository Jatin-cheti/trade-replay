/** TV-Parity 500 Tests — EXTENDED LINE (2 anchors, infinite both directions). */
import { register500ToolSuite } from "./tv-parity-500-factory";
register500ToolSuite({ variant: "extendedLine", testId: "tool-extended-line" });
